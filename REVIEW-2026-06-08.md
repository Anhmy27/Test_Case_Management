# Senior Code Review — Test Case Management (2026-06-08)

Fresh review against the **current** code. The earlier `REVIEW.md` is partly stale —
several issues it flagged are now fixed (Jira routes are authenticated, `jiraController`
has a single export, Excel import has size/MIME checks, the automation-results route now
sits behind auth).

## Update log (2026-06-09)

### Done in code

- **Regex injection / ReDoS in search**: fixed in `backend/src/utils/versioning.js`.
  - Search input is now regex-escaped before building `$regex`.
- **Delayed plans wrong with versioned plans**: fixed in `backend/src/services/testRunDashboardService.js`.
  - Delayed-plan detection now compares by `testPlanEntityId` (with fallback mapping from `testPlan` to `entityId`).
- **`$regex` table-scan risk (partial mitigation)**:
  - Search moved from contains regex to **prefix regex** (`^...`) in `buildSearchMatch`.
  - Added missing indexes on searched text fields:
    - `Project.description`
    - `Version.notes`
    - `TestCaseGroup.description`
    - `TestCase.description`
    - `TestPlan.description`

### Progress sync (latest, 2026-06-09 PM)

Compared against the findings in this file:

#### Newly completed since the last review snapshot

- **#9 No input validation layer**: completed.
  - Zod schemas + centralized `validateRequest` middleware are now applied across routes (`auth`, `user`, `jira`, `test management`, `test-runs`, `automation dry-run`).
  - Validation errors are normalized via `errorMiddleware` (`Validation failed` + `details`).
- **#110 applyAutomationResults auth concern**: completed.
  - Automation ingest now uses dedicated middleware (`authenticateAutomationIngest`) and the service no longer performs ad-hoc header checks.
- **Employee History filtering correctness (post-review functional fix)**:
  - History now shows only runs where the employee is both **assignee/owner on the plan snapshot** and **the same user who started the run**.
- **Entity identity consistency hardening (post-review functional fix)**:
  - `_id` is preserved as true document id for runtime/run flows; `entityId` is now explicitly returned/used as canonical business id in affected dashboard/service payloads.
- **Test Plan create modal version selector bug (post-review functional fix)**:
  - Version options now resolve correctly under project scope using canonical entity matching.
- **Start run UX false-positive warning (post-review functional fix)**:
  - After successful run creation, form state is reset to avoid stale duplicate-name warning.
- **#8 `testManagementService.js` is too large**: completed.
  - Removed `backend/src/services/testManagementService.js` and completed domain split into:
    - `projectVersionServices.js`
    - `issueTypeGroupServices.js`
    - `testCaseServices.js`
    - `testPlanServices.js`
  - Shared versioning/resolver helpers moved to `backend/src/services/shared/`.
- **#12 Jira integration approach (`login.jsp` / session flow)**: accepted as correct for this environment.
  - Reviewer assumed cloud Jira + REST API tokens; production Jira here is **self-hosted legacy Java/JSP**.
  - Official REST-only integration is not available for the required create-issue flow; backend must authenticate via `login.jsp`, obtain session cookie/`atl_token`, and post to internal `*.jspa` endpoints.
  - Current proxy design in `jiraService.js` matches platform constraints; not treated as an open refactor item unless Jira is upgraded or exposes a supported API.

#### Partial progress

- **Testing gap**: partial.
  - Backend now has validator-focused tests (`backend/test/validators.test.js`) and `npm test` runs via `node --test`.
  - Broader service/integration coverage is still missing.

#### Still open (unchanged)

- **#1 Secrets in git / env hygiene**.
- **#2 JWT hardening and revocation strategy**.
- **#3 Move auth token out of localStorage**.
- **#4 Close or gate self-registration**.
- **#5 Rate limiting for auth/Jira and sensitive routes**.
- **#7 Durable automation queue/recovery (replace in-memory runner)**.
- **#10 Production error sanitization + enforced `NODE_ENV=production`**.
- **#11 Artifact storage durability/retention strategy**.
- **#13 SSRF guardrails for automation baseUrl/upload path constraints**.
- **CI/CD, observability, audit log, backup/ops checklist in section "Missing for production use"**.

### Still open / next targets

1. Secrets in git + `.gitignore` fix + rotate credentials.
2. JWT hardening (no fallback secret, shorter TTL, revocation strategy).
3. Move auth token out of `localStorage` (httpOnly cookie + CSRF/CSP).
4. Close/gate open self-registration.
5. Add rate limiting on auth/Jira routes.
6. Set `NODE_ENV=production` and sanitize upstream Jira error bodies.

## Overall assessment

For a fresher + AI effort the structure is genuinely good: clean route → controller →
service layering, a soft-delete + versioning abstraction, a reasonable React component
split, and sensible Mongoose models. The gaps are the expected ones — **security
hardening, durability, validation, and testing** — none of which are production-ready yet.

---

## 🔴 Critical — must fix before any deployment

1. **Real secrets committed to git** (`backend/.env`). Tracked file contains a live Jira
   password, `JWT_SECRET`, and admin password. `.gitignore` has `.env/` (trailing slash →
   ignores a *directory*, not the file), so it never worked.
   - Rotate the Jira password and JWT secret now (they are in git history).
   - `git rm --cached backend/.env`, fix `.gitignore` to `.env`, add a `.env.example`.
   - Treat history as compromised; consider `git filter-repo` to purge.

2. **Weak/default JWT secret + no revocation** (`authMiddleware.js:6` falls back to
   `'replace-me-secret'`; `.env` ships `super-secret-change-me`). Tokens are 7-day,
   stateless, and cannot be revoked — a deactivated/compromised user keeps access for a
   week. Fail-fast if `JWT_SECRET` is unset; shorten lifetime; add refresh/revocation.

3. **JWT stored in `localStorage`** (`WorkspaceShell.tsx`, `app/page.tsx`). Any XSS = full
   account takeover. Move to httpOnly cookies (+ CSRF) or add a strict CSP. No `helmet`
   today.

4. **Open self-registration** (`POST /api/auth/register`). Anyone on the network can create
   an `employee` account — no verification/invite/approval. Remove or gate it.

5. **No rate limiting anywhere.** Login, register, and the Jira proxy are brute-forceable.
   Add `express-rate-limit` on `/api/auth/*` at minimum.

6. **Regex injection / ReDoS in search** (`utils/versioning.js`) — **fixed 2026-06-09**.
   Input is escaped before `$regex` is built.

---

## 🟠 High-priority — architecture & robustness

7. **Automation job runner is in-memory and non-durable** (`automationJobRunner.js`).
   `activeRunIds` is a module-level `Set`; jobs run via `setImmediate`. A restart mid-run
   leaves `TestRun.status = 'running'` forever (no reaper); won't work with >1 instance; no
   real queue/retry/concurrency cap. Use a persistent queue (BullMQ/Redis) + a startup
   reconciler for orphaned runs.

8. **Service decomposition for test management domain** — **fixed 2026-06-09**.
   The legacy `testManagementService.js` aggregation layer was removed and replaced with domain-specific service entrypoints plus shared helpers.

9. **No input validation layer.** Controllers pass `req.body`/`req.query` straight into
   services. Add schema validation (Zod/Joi) at the route boundary.

10. **Error stack traces leak by default** (`errorMiddleware.js`). `NODE_ENV` is never set,
    so prod will leak stacks and raw upstream Jira HTML error bodies. Set
    `NODE_ENV=production` and sanitize upstream error bodies.

11. **Local-disk artifact storage** (`artifactStore.js`, failure screenshots). Breaks with
    multiple replicas / ephemeral containers; no retention/cleanup. Use object storage.

12. **Jira integration via `login.jsp` / internal JSP endpoints** — **accepted 2026-06-09**.
    Self-hosted legacy Jira (Java/JSP) requires session login and `atl_token` from
    `login.jsp`; REST API token flow is not a viable replacement for the current create-issue
    path. Kept as intentional design for this deployment target.

13. **SSRF surface via automation** (`runOrchestrator.js` / dry-run). Admin-supplied
    `baseUrl`/`goto` drive a server-side headless browser to arbitrary URLs; the `upload`
    action (`playwrightExecutor.js:280`) resolves arbitrary server filesystem paths. Add a
    `baseUrl` allowlist and disallow absolute server paths.

---

## 🟡 Bugs & smaller issues

- **Default admin seeded from `.env`** (`seedAdmin.js`) with `admin@example.com` /
  `12345678`. Don't ship defaults; require explicit opt-in.
- **`applyAutomationResults` has no role authorization** — authenticated only + an
  `x-automation-secret` header check. Confirm the secret check rejects when missing.
- **Bleeding-edge deps**: Express 5, Mongoose 9, Next 16, React 19. Pin and verify for prod.
- **No DB index review** beyond versioning defaults; `$regex` searches table-scan. **Partially mitigated 2026-06-09** with prefix regex + added indexes on searched description/notes fields. Full text index strategy still recommended.

---

## 🟢 Missing for production use

- **Tests are still thin** (`npm test` now runs validator-focused Node tests). Add service + integration coverage for auth, versioning, automation runner.
- **CI/CD**: no pipeline, no lint/typecheck gate, no app Dockerfile (compose only runs Mongo).
- **Observability**: no structured logging, request logging, metrics, or error tracking.
- **Health/readiness endpoint** for orchestration (only `/` banner).
- **Account lifecycle**: no password reset, email verification, complexity policy, lockout.
- **Audit log** of who changed/ran what.
- **Backups & retention** for Mongo and artifacts.
- **HTTPS/TLS + reverse proxy** guidance; **graceful shutdown** (drain runs, close Mongo).

---

## Suggested order of attack

1. Rotate + purge secrets, fix `.gitignore`, fail-fast on missing `JWT_SECRET`. (hours)
2. Add `helmet` + `express-rate-limit` + close/gate self-registration. (hours)
3. Escape regex search input; set `NODE_ENV=production` + sanitize upstream error bodies. (hours)
4. Add a startup reconciler for orphaned "running" runs; plan a real job queue. (days)
5. Expand automated tests to service/integration level and add CI gates. (days)

## Verification evidence (2026-06-09)

- Backend unit tests pass: `cd backend && npm test` -> 7/7 passing.
- Backend module smoke checks pass after service split:
  - Controllers and routes load without import/runtime errors.
  - New domain service entrypoints (`projectVersionServices`, `issueTypeGroupServices`, `testCaseServices`, `testPlanServices`) load successfully.
- Frontend production build passes: `cd frontendnext && npm run build` (TypeScript + route generation successful).
