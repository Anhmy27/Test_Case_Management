# frontendnext

This is the active frontend for the Test Case Management System.

For the full project guide, backend setup, MongoDB, and automation notes, see the root [README.md](../README.md).

Quick start:

```bash
npm install
npm run dev
```

Open `http://localhost:3000` after the dev server starts.