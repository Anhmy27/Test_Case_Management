# Normalize text files to LF in the repo (Linux/Mac/CI/Node default).
# Windows scripts that require CRLF are listed explicitly below.
* text=auto eol=lf

*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
